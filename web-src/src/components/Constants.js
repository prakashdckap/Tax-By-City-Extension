/**
 * Admin UI SDK extension id: must match `extensionId` in src/admin-ui/actions/registration/index.js
 * and the commerce extension metadata (app.commerce.config.mjs). Use only [a-z0-9_] for menu ids.
 */
export const extensionId = 'taxbycity'
