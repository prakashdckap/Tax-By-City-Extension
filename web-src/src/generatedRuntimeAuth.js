/**
 * Overwritten on each `npm run build` / `deploy` by scripts/inject-runtime-auth-for-web.mjs
 * (reads .env: RUNTIME_AUTH_BASE64 or RUNTIME_USERNAME + RUNTIME_PASSWORD).
 * This stub keeps imports working in the editor before inject runs.
 */
export const RUNTIME_BASIC_AUTH_BASE64 = ''
