/** Commerce app metadata (Exchange / project). `id` aligns with Admin UI registration + Constants.extensionId. */
export default {
  metadata: {
    id: "taxbycity",
    displayName: "Tax By City",
    version: "0.0.1",
    description: "Tax by city for Adobe Commerce"
  }
};
